{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": ["lib/*"],
  "scripts": {
    "build": "tsc --build",
    "start": "node lib/db/dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
  "scripts": {
    "build:api": "pnpm -r --filter @workspace/api-server run build",
    "start": "node artifacts/api-server/dist/index.mjs"
  }
}
