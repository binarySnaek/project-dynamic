{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": ["lib/*"],
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  },
  "scripts": {
  "build:api": "pnpm -r --filter @workspace/api-server run build",
  "build:frontend": "pnpm -r --filter @workspace/science-research-portal run build",
  "build:all": "pnpm run build:api && pnpm run build:frontend"
}
}
